extends Node

signal impacted
signal reflected
signal spaghettified
signal drift_off
signal launched
signal reset
signal win
signal level_loaded
signal next_level
signal fade_fin
signal index_level

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
